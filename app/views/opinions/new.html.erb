<%= form_for new_matchup_opinion_path do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.check_box :vote, {}, 1, 0 %>
  <%= f.check_box :vote, {}, -1, 0 %>

  <%= f.submit %>

<% end %>
